# SRD content dumps

Drop `monsters.json`, `spells.json`, and `items.json` here in the internal `ContentRecord` shape, then run `pnpm ingest`.

The fixture world already seeds a small playable subset. This directory is the path for the full SRD 5.1 dump and, later, a `PostgresAdapter` reseed from the existing 5e database.
